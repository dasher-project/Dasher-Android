#This file is generated by updateDaemonJvm
#Vendor pin removed so CI (and contributors on Linux/macOS) can use any JDK 21
#(e.g. the Temurin 21 provisioned by setup-java) instead of only JetBrains JBR,
#which Gradle can't auto-download for LINUX x86_64.
toolchainVersion=21
